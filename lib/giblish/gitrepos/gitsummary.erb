= <%= @repo_name %> Docs

Generated at <%= DateTime.now %>

== Summary

<% unless @branch_info.empty? %>
.git Branches
[cols="1a,5a"]
|===

<% @branch_info.each do |b|
  c = b.latest_commit
%>
2+|xref:<%= index_path(b.name) %>[*<%= b.name %>*] _last committed to <%= c.datetime.strftime("%Y-%m-%d") %> by <%= c.committer %>._

|*Sha* 
|<%= c.sha[0..7] %> ...

|*Message*
|<%= c.message %>
<% end %>

|===
<% end %>

<% unless @tag_infos.empty? %>
.git Tags
[cols="1a,5a"]
|===

<% @tag_infos.each do |t| 
  c = t.commit
%>

2+|xref:<%= index_path(t.name) %>[*<%= t.name %>*] _was created <%= t.date.strftime("%Y-%m-%d") %> by <%= t.author %>._

|*Message*
|<%= t.message %>

2+|_The tag points to the commit made <%= c.datetime.strftime("%Y-%m-%d") %> by <%= c.committer %>._

|*Sha*
|<%= c.sha[0..8] %> ...

|*Message*
|<%= t.commit.message %>

<% end %>
|===
<% end %>

Generated by 'giblish - v<%= Giblish::VERSION %>'

